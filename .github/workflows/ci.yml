name: ci

on: [push]

jobs:

  build_in_docker:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    - name: Build docker image
      run: docker build -t docker_img .
    - name: Run tests in docker
      run: docker run --rm docker_img:latest
